# initial checkin
- type: parallel
  name: example_parallel_step
  steps:
    - service: codeship_pybot
      command: --version
    - service: codeship_example
      command: --help 
- name: example_tagged_step
  tag: master
  service: codeship_dryrun
  command: --dryrun  *.txt.robot 
